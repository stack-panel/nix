# Core stackpanel options
# Note: perSystem options are defined in generate.nix
# This file is for flake-level (non-perSystem) options if needed
{ lib, ... }:
{
  # Flake-level options would go here
  # For now, everything is perSystem
}
